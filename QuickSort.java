import java.util.ArrayList;

class QuickSort {
    public static ArrayList<Integer> quickSort(ArrayList<Integer> arr) {
        if (arr.size() <= 1)
            return arr;

        ArrayList<Integer> smallerElementsArr = new ArrayList<>();
        ArrayList<Integer> biggerElementsArr = new ArrayList<>();
        return null;

    }
}